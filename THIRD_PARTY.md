# Third-Party Notices

This repository depends on open-source packages declared in `package.json` and `package-lock.json`, including:

- `node-pty`
- `playwright-core`
- `tar`
- `zod`
- `@modelcontextprotocol/sdk`

Refer to each package's upstream license for full terms.
